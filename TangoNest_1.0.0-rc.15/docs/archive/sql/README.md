# Archived SQL

These migrations are retained only for historical reference.

Do not run them on the current TangoNest project. Use the root-level
`SUPABASE_SCHEMA_CURRENT.sql` as the single canonical schema and migration.
