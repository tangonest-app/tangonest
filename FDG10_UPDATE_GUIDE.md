# TangoNest FDG10 更新・確認手順

## 現在の状態

このファイルはFDG10公開用パッケージに含まれています。公開反映は下記の版表示で確認してください。
実際のSupabase認証・権限と、実機PWAアイコンの最終受け入れ確認が必要です。
今回、公開データベースの単語・プレイリストは削除していません。

## 1. 古い自動リセットを無効化

2026年9月6日、対象プロジェクトのSQL Editorで `SUPABASE_FDG10_SAFETY.sql` 相当の安全修正を適用済みです。ユーザーによる再実行は不要です。
旧アプリから呼ばれ得る2個の自動リセット処理を「データを変更しない処理」に置き換えます。
繰り返し実行できます。単語・学習履歴・アカウントを削除するSQLではありません。
適用前後とも全体の件数は1,644単語・3プレイリストでした。旧関数の削除処理がなくなり、匿名実行不可・認証済み実行可の権限が維持されていることを確認しています。

過去版のRESET用SQLは実行しないでください。既存FDG9環境に対して、フルスキーマSQLを追加実行する必要はありません。

## 2. 公開前の実認証チェック

本番の個人データとは分けたテスト用アカウントを使い、ログイン、再読み込み、別ブラウザでの復元、ログアウト、再ログインを確認します。
2つのテスト用アカウントで互いの単語・プレイリストが表示されないことも確認します。
開発用ソースには `test:integration:auth` があります。パスワードは配布ファイルやGitHubに記載しないでください。

単語とプレイリストの読み取りにエラーがある場合、公開を完了扱いにせず、詳細を確認してください。
画面幅のシミュレーションやモック認証テストは、この実認証確認の代わりにはなりません。

## 3. GitHub Pagesへ更新

公開対象は `TangoNest_FDG10_REVIEW_CANDIDATE` フォルダの**中身**です。ZIPファイル自体や、フォルダをもう1段入れた形では公開しません。
GitHubリポジトリの現在のPages公開元に合わせて、同じ場所の `index.html` と各フォルダを更新してください。
ソース全体、`node_modules`、テスト結果、`qa` はアップロード不要です。

既存の独自ドメイン設定やGitHub Actions設定は、そのまま保持してください。
公開処理の成功後、公開URLの末尾 `/tangonest/` から開きます。

## 4. 更新確認

1. 更新通知が出た場合は「Update」を押します。Bulk Add中は完了または停止後に行います。
2. Settingsの版表示が **1.0.0-rc.19-fdg10** であることを確認します。
3. Add WordsのBulk Addに「Choose format」「Copy prompt for ChatGPT」「Copy format」「Insert sample」が表示されることを確認します。
4. 以前からの単語・プレイリストが残り、再読み込み後もログイン状態が維持されることを確認します。
5. Library、Cards、Quiz、Listenへ移動し、ブラウザの戻る・進むも確認します。

FDG9のままなら、公開元のファイルと公開処理を先に確認してください。Cookieや全サイトデータを消して解決したことにはしないでください。

## 5. ホーム画面アイコン

新規のfavicon、Apple Touch Icon、通常PWA用、maskable用は同じ最新マスターから生成しています。
iOS/iPadOSで既にホーム画面へ追加したアイコンは、OS側が以前の画像を保持する場合があります。
新規追加との比較で確認してください。再追加が必要な場合も、先に同期完了を確認し、端末上だけの未同期データを消さないでください。

左上の異物の症状は実機で再現確認できていません。最新マスターの絵柄を勝手に加工して隠してはいません。
再現する場合は、OSバージョン、新規追加か既存アイコンか、問題のスクリーンショットを揃えて確認します。
